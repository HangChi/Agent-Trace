{
  "name": "@agent-trace/desktop-tauri",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "build": "tauri build",
    "build:exe": "tauri build --no-bundle",
    "dev": "tauri dev",
    "lint": "node scripts/check-ui.mjs && cargo clippy -p agent-trace-desktop --all-targets -- -D warnings",
    "test": "node scripts/check-ui.mjs && cargo test -p agent-trace-core"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1"
  }
}
