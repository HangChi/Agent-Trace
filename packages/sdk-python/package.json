{
  "name": "@agent-trace/sdk-python",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "python -m compileall -q agent_trace",
    "lint": "python -m compileall -q agent_trace tests",
    "test": "python -m unittest discover -s tests -v",
    "typecheck": "python -m compileall -q agent_trace tests"
  }
}
